"""Evaluation harness: scoring, results I/O, and the run-the-matrix runner.

Built slice by slice in P2: `scoring.py` (parse + score) lands in P2.1, results
persistence + resume in P2.2, and the config-driven, shardable `runner.py` in P2.3.
"""
