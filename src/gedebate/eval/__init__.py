"""Evaluation harness: scoring, results I/O, and the run-the-matrix runner.

`scoring.py` parses + scores an answer, `results.py` owns the persistence schema and
resume, and `runner.py` is the config-driven, shardable run loop.
"""
